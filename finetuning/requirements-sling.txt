# Linux / SLING finetuning stack.
# Install torch separately so it matches the GPU driver/CUDA runtime on the
# allocated node.
#
# H100/A100-class example:
#   python -m pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128
#
# V100S-safe example:
#   PYTHONNOUSERSITE=1 PIP_NO_USER=1 python -m pip install --force-reinstall torch==2.7.1 --index-url https://download.pytorch.org/whl/cu118

transformers==5.5.0
datasets==4.8.4
peft==0.18.1
accelerate==1.13.0
tensorboard==2.20.0
PyYAML==6.0.3
beautifulsoup4==4.14.3
pypdf==6.9.1
requests==2.33.1
sentencepiece==0.2.1
bitsandbytes==0.49.2
